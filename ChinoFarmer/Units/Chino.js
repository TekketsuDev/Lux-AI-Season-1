const kit = require('./lux/kit');
const GAME_CONSTANTS = require('./lux/game_constants');
const DIRECTIONS = GAME_CONSTANTS.DIRECTIONS;

const helpers = require('./ChinoFarmer/helpers');

agent.initialize().then(async () => {
	while (true) {
		/** Do not edit! **/
		// wait for updates
		await agent.update();

		const actions = [];
		const gameState = agent.gameState;

		const player = gameState.players[gameState.id];
		const gameMap = gameState.map;




	function avariableWorkersPos(unit, game_state) {
			if (unit.isWorker() && unit.canAct()) {
					
			}
	}

	function find_Resoruces(game_state) {
			let avariablePos = null;
			
	}

	}
});
