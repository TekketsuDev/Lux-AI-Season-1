const WorkerRole = {
    Gatherer: 0,
    Frontier: 1,
    ChinoFarmer: 2,
}

switch(WorkerRole) {
    case WorkerRole.Gatherer:
        // Do GathererRole
        break;
    case WorkerRole.ChinoFarmer:
        // Do ChinoFarmer
        break;
    case WorkerRole.Frontier:
        //Do Fronterize
        break;
}